name: Build EduKI Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Configurar JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Configurar Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar dependências
        run: npm install

      - name: Instalar Capacitor CLI globalmente
        run: npm install -g @capacitor/cli

      - name: Build da web (dist)
        run: npm run build

      - name: Adicionar plataforma Android (cria a pasta android/)
        run: npx cap add android

      - name: Sync Capacitor (copia web + plugins Unity Ads)
        run: npx cap sync android

      - name: Tornar Gradle executável
        run: cd android && chmod +x ./gradlew

      - name: Build APK debug
        run: cd android && ./gradlew assembleDebug

      - name: Build AAB release
        run: cd android && ./gradlew bundleRelease

      - name: Upload APK debug
        uses: actions/upload-artifact@v4
        with:
          name: EduKI-debug-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload AAB release
        uses: actions/upload-artifact@v4
        with:
          name: EduKI-release-AAB
          path: android/app/build/outputs/bundle/release/app-release.aab
