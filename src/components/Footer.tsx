const Footer = () => {
  return (
    <footer className="fixed bottom-0 left-0 right-0 py-2 text-center text-xs text-muted-foreground/50 bg-transparent pointer-events-none z-40">
      App criado por GTECHS
    </footer>
  );
};

export default Footer;
